### 项目简介
本项目测试，由ai生成一个可用的api对话系统。


### 使用方案：
下载项目到本地，然后`npm run build`，`npm run preview -- --host`,手机或者其他设备连接到该端口，然后通过PWA功能缓存到本地。

但是还没有成功。目前可以在windows上，通过chrome、edge下载。但是在其他设备，如手机，怀疑是系统或者浏览器不支持，也可能是PWA的安全机制，导致没有成功